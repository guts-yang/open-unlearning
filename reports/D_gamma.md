# D-γ 扫描报告（Stage 2 支线，方案 §6 机遇 3）

零训练成本诊断：SimNPO 的效用崩溃是「漂移」（可恢复）还是「真删除」（不可逆）？

## 配置

- θ₀：`open-unlearning/tofu_Llama-3.2-1B-Instruct_full`（占位，以实际运行为准）
- θ_SimNPO：`—`（官方 ckpt，forget10，ep5/ep10；本机 HF 不可达，待远程拉取）
- retain_logs_path：`—（未提供，D_KS=None）`（G0 产物的冻结 retain TR JSON）
- 设备：`—`

## (γ, D_KS, MU) 轨迹

| γ | D_KS | MU |
|---|---|---|
| 0.5 | — | — |
| 0.75 | — | — |
| 1.0 | — | — |
| 1.25 | — | — |
| 1.5 | — | — |

> 状态：本机交付时未运行（无 GPU / HF 不可达），数字为 `—` 占位；
> 远程 2×A800 跑完 `scripts/d_gamma_scan.py` 后自动回填。

## 判定（方案 §6 机遇 3，三行表原文照抄）

| 结果 | 解读 | 对搜索空间的影响 |
|---|---|---|
| `γ<1` 时 MU 回升而 `D_KS` 不变 | **崩溃主要是漂移** | ⟹ ES 的任务不是「找方向」，而是**沿 Δ_FO 射线做步长/曲率修正** ⟹ 搜索空间缩到 `S ∪ {Δ_FO}`，**K+1 维**，成本再降一个量级 |
| 两者同步变化 | 真帕累托前沿 | ES 必须做全 K 维搜索 |
| `γ>1` 外推 `D_KS` 继续降 | 方向对、步长不够 | ⟹ **「遗忘方向」与「步长」是可分离的两个问题**——这本身就是一个可发表的发现 |

**初步判定**：数据缺失（retain_logs_path 未提供或指标返回 None），无法判定，待远程补跑。

## 对 Stage 5 搜索空间的约束

- 若判定为**漂移为主**：ES 的任务从「找方向」改为「沿 Δ_FO 射线做步长/曲率修正」，搜索空间缩为 `S ∪ {Δ_FO}`（**K+1 维**），种群 ES 的维度与成本再降一个量级。
- 若判定为**真帕累托**：ES 必须做全 K 维搜索，`accumulate_gram` 的 K 取 G1′ 定出的 `K_eff`。
- 若判定为**步长不足**：「遗忘方向」与「步长」解耦，`σ_t` 衰减调度只需管 `ρ_cross`（主方案 §8），方向基用 `G^{-1/2}` 白化后的 `S`。

> 技术先例（主方案 §6 机遇 3）：Hoy et al. 已证明 ES 与 GRPO 的解线性连通且无损失壁垒 ⟹ 权重插值是合法的分析工具。
